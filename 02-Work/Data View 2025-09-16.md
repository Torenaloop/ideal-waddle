const tasks = dv.pages()
  .flatMap(p => p.file.tasks.map(t => ({...t, ctime: p.file.ctime})))
  .filter(t => !t.completed)
  .sort(t => t.ctime, 'asc');
dv.taskList(tasks, {shortMode: false});

```datviewjs
const tasks = dv.pages()
  .flatMap(p => p.file.tasks.map(t => ({...t, ctime: p.file.ctime})))
  .filter(t => !t.completed)
  .sort(t => t.ctime, 'asc');
dv.taskList(tasks, {shortMode: false});

```

```dataview
TASK
WHERE !completed
SORT file.ctime ASC

```
#OpsEHS 
 #Work/Goal/2025
 