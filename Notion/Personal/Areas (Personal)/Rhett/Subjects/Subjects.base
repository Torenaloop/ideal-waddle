# Subjects

filters:
  and:
    - note["base"] == link("Subjects.base")
properties:
  file.name:
    displayName: Areas (Rhett)
  Tags:
    displayName: Tags
views:
  - type: table
    name: Table View
    order:
      - file.name
      - Tags
