# New database

filters:
  and:
    - note["base"] == link("New database.base")
properties:
  file.name:
    displayName: Name
views:
  - type: table
    name: Table View
    order:
      - file.name
